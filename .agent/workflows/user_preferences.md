---
description: User preferences for agent behavior and development workflow
---

# User Preferences

1. **Browser Interaction**: Do not automatically open the browser or verify previews (e.g., using `browser_subagent`) unless the user explicitly requests it. Trust the code changes and user verification.
